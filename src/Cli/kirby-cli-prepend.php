<?php

declare(strict_types=1);

// Prevent Kirby from defining conflicting global helpers (e.g. `e()`).
if (!defined('KIRBY_HELPER_DUMP')) {
    define('KIRBY_HELPER_DUMP', false);
}

if (!defined('KIRBY_HELPER_E')) {
    define('KIRBY_HELPER_E', false);
}
